# Docker Registry Proxy 环境变量配置
# 使用方法：
#   1. 复制此文件: cp .env.example .env
#   2. 修改下面的配置项
#   3. 启动服务: docker-compose up -d

# ==================== 必须修改 ====================

# TODO: 修改为您的实际域名
REGISTRY_DOMAIN=docker-proxy.yourdomain.com

# ==================== Traefik 配置 ====================

# TODO: 确认 Traefik 网络名称（查看方法: docker network ls）
TRAEFIK_NETWORK=traefik-net

# TODO: 确认 Traefik certresolver 名称
# 在您的 Traefik 配置中查找 certificatesResolvers 的名称
TRAEFIK_CERTRESOLVER=letsencrypt

# TODO: 确认 Traefik HTTPS entrypoint 名称
# 常见值: websecure, https
TRAEFIK_ENTRYPOINT=websecure

# Registry 日志级别 (debug, info, warn, error)
REGISTRY_LOG_LEVEL=info

# Rate Limiting 配置（可选，在 nginx.conf 中调整）
# 每秒请求数限制
RATE_LIMIT_RPS=10
# 突发请求数
RATE_LIMIT_BURST=20
# 并发连接数限制
RATE_LIMIT_CONN=10

# 如果需要代理私有镜像，配置 Docker Hub 凭证（不推荐）
# DOCKERHUB_USERNAME=your-username
# DOCKERHUB_TOKEN=your-access-token
